<%= form_with(model: smiley) do |form| %>
  <% if smiley.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(smiley.errors.count, "error") %> prohibited this smiley from being saved:</h2>

      <ul>
        <% smiley.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
